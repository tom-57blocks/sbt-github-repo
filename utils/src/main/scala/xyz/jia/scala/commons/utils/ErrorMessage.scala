package xyz.jia.scala.commons.utils

/** Tinny class for explicitly communicating that a text being returned is an error message
  * @param message
  *   The error text
  */
case class ErrorMessage(message: String)
